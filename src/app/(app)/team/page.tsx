/**
 * Team.tsx
 *
 * Client-side page that renders the shared <Team /> component.
 * Acts as the route entry point for /team.
 */

"use client";

import Team from "@/components/Team";

export default function TeamPage() {
  return <Team />;
}
