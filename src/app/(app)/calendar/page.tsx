/**
 * Calendar.tsx
 *
 * Client-side page that renders the shared <Calendar /> component.
 * Acts as the route entry point for /calendar.
 */

"use client";
import {Calendar} from "@/components/Calendar";

export default function CalendarPage() {
  return <Calendar />;
}
