/**
 * Settings.tsx
 *
 * Client-side page that renders the shared <Settings /> component.
 * Acts as the route entry point for /settings.
 */

"use client";

import {Settings} from "@/components/Settings";

export default function SettingsPage() {
  return <Settings />;
}
