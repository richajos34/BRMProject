/**
 * Dashboard.tsx
 *
 * Client-side page that renders the shared <Dashboard /> component.
 * Acts as the route entry point for /dashboard.
 */

"use client";

import { Dashboard } from "@/components/Dashboard";

export default function Home() {
  return (
      <Dashboard />
  );
}
