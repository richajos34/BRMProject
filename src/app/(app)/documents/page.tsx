/**
 * Documents.tsx
 *
 * Client-side page that renders the shared <Documents /> component.
 * Acts as the route entry point for /documents.
 */

"use client";

import {Documents} from "@/components/Documents";

export default function DocumentsPage() {
  return <Documents />;
}
